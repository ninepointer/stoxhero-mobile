import 'package:flutter/material.dart';

class CollegeContestView extends StatelessWidget {
  const CollegeContestView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Contests Leaderboard'),
      ),
    );
  }
}
