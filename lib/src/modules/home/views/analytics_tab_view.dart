import 'package:flutter/material.dart';

class AnalyticsTabView extends StatelessWidget {
  const AnalyticsTabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AnalyticsTabView'),
    );
  }
}
