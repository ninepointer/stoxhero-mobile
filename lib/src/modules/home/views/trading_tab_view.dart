import 'package:flutter/material.dart';

class TradingTabView extends StatelessWidget {
  const TradingTabView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('TradingTabView'),
    );
  }
}
