import 'package:flutter/material.dart';

class MarginxEndView extends StatelessWidget {
  const MarginxEndView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('MarginX End'),
      ),
      body: Container(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
