import 'package:flutter/material.dart';

class MarginxView extends StatelessWidget {
  const MarginxView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
