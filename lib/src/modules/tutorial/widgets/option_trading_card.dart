import 'package:flutter/material.dart';

class OptionTradingCard extends StatelessWidget {
  const OptionTradingCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
